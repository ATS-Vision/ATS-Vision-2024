#ifndef TOOL_ENUM_HPP_
#define TOOL_ENUM_HPP_

#include <iostream>

enum TargetType
{
    SMALL,
    BIG,
    BUFF
};
enum SpinHeading
{
    UNKNOWN,
    CLOCKWISE,
    COUNTER_CLOCKWISE
};

enum EnemyColor
{
    BLUE,
    RED,
    GRAY,
    PURPLE
};
#endif